import Core;
//import Map;
//import <iostream>;

int main()
{
	//Map m{"Test"};

	//for ( int y{}; y < 30; ++y ) {
	//	for ( int x{}; x < 30; ++x )
	//		std::cout << m.code[y][x] / 1000;
	//	std::cout << std::endl;
	//}

	//system( "pause" );

	Core::Run();
}